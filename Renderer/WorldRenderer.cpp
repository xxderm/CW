#include "WorldRenderer.h"

void WorldRenderer::Render()
{
}

void WorldRenderer::Update()
{
}

void WorldRenderer::Init()
{
	/*for (size_t y = 0; y < 50; y++)
	{
		for (size_t x = 0; x < 128; x++)
		{
			mObject.Terrain.push_back(glm::vec3(x, 0, y));
			mObject.TexCoord.push_back(glm::vec2(mObject.Terrain.back().x, mObject.Terrain.back().z));
			mObject.Indices.push_back(mObject.Terrain.size() - 1);

			mObject.Terrain.push_back(glm::vec3(x + 1, 0, y));
			mObject.TexCoord.push_back(glm::vec2(mObject.Terrain.back().x, mObject.Terrain.back().z));
			mObject.Indices.push_back(mObject.Terrain.size() - 1);

			mObject.Terrain.push_back(glm::vec3(x + 1, 0, y + 1));
			mObject.TexCoord.push_back(glm::vec2(mObject.Terrain.back().x, mObject.Terrain.back().z));
			mObject.Indices.push_back(mObject.Terrain.size() - 1);

			mObject.Terrain.push_back(glm::vec3(x, 0, y + 1));
			mObject.Indices.push_back(mObject.Terrain.size() - 1);
			mObject.TexCoord.push_back(glm::vec2(mObject.Terrain.back().x, mObject.Terrain.back().z));
			mObject.Indices.push_back(0xFFFF);
		}
	}*/
}
